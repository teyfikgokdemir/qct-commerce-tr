# RexCode-inspired interaction check

QCT Commerce local preview: `http://127.0.0.1:4322/`

The Turkish header now exposes `Hizmetler` and `Çalışmalar` as native button-controlled disclosure groups. The Services panel contains all seven commercial solution routes; the Work panel contains `Çalışmaları gör` and `Proje başlat`. The local accessibility tree presents the group controls as buttons and destinations as links.

The panel uses the existing compact dark navigation surface, with a two-column desktop grid, short opacity/translate opening, focused link cards and the existing outside-click/Escape/keyboard behavior from the site menu script. The hero remains a direct commercial composition with the primary CTA `Projenizin önceliğini konuşalım` and secondary `Hizmetleri inceleyin`; no heavy continuous hero animation was added.

Build result: 42 pages generated. Multilingual canonical, metadata, schema and internal-link audit passed. Redirect audit passed with 110 safe 301 mappings, 10 approved wildcard families, and no redirect chain or loop.
